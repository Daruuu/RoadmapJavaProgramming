package NoGit.PracticaExamen;

import java.util.Scanner;

public class Exercise1 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();

//        int espacios = str.replaceAll("[^ ]", "").length();
//        System.out.println("espacios eliminados en el string: " + espacios);

//        int contador = 0;
//        for (int i = 0; i < str.length(); i++) {
//            if (Character.isWhitespace(str.charAt(i))) {
//                str.replaceAll("[^ ]", "");
//                contador++;
//            }
//        }
//        System.out.println("espacios en string: " + contador);

//        int countVowels = 0;
//        for (int i = 0; i < str.length(); i++) {
//            if (str.charAt(i) == 'a' || str.charAt(i) == 'e' || str.charAt(i) == 'i' || str.charAt(i) == 'o'
//                    || str.charAt(i) == 'u') {
//                countVowels++;
//
//            }
//        }
//        System.out.println("vocales en string: " + countVowels);

//        String nuevoStr = str.replaceAll("[aeiou]","");
//        System.out.println(nuevoStr +" shan suprimit " + espacios + " i " + countVowels + " vocals.");
    }
}