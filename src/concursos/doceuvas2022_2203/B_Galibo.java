package concursos.doceuvas2022_2203;

import java.util.Scanner;

public class B_Galibo {
    // numero de puentes
    // numero de carriles
    // galibo en trenes: considerar el alto, el ancho a distintas alturas

    //INPUT:
    // n casos
    // numero de puentes que hay en trayecto ( hasta 100)
    // numero de carriles que pasan debajo del puente(max 5)
    // altura entre (1- 800) cm

    // 2
    // 3 300 300 250
    // 2 325 300
    // OUTPUT
    // altura maxima
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int numCasos = sc.nextInt();
        while (numCasos != 0){

            int numeroDePuentes = sc.nextInt();

            while (numeroDePuentes != numCasos){
                sc.nextInt();
            }
            int numeroCarriles;
            numCasos =sc.nextInt();
        }
    }
}
